<div id="where-list">
  <table class="striped-table">
    <thead>
      <th>Name</th>
      <th>Team</th>
      <th>Where</th>
      <th>How?</th>
      <th>Email</th>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <% where = where_or_stub_for(user) %>
        <tr>
          <td><%= user.name %></td>
          <td><%= user.team_name %></td>
          <td>
            <%= where.body %>
            <div class='description'><%= time_since(where.sent_at) %></span>
          </td>
          <td><%= where.provenance %>
          <td><%= user.email %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
