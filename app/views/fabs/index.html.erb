<h2 class="team-name-header"><%= @user.team_name %></h2>
<%= render partial: '/users/user_details', locals: {user: @user}%>

<!-- Fab Entry -->
<%= render 'form' %>
<br>

<div class="fabs-back">
  <% @fabs.drop(1).each do |fab| %>
    <div class="fab">
      <%= render partial: 'index_fab_back', locals: {fab: fab} %>
    </div>
  <% end %>
</div>
